package com.realestateserver.server.Controller;

import org.springframework.web.bind.annotation.*;


@RestController
@CrossOrigin
@RequestMapping("api/v1/agent")

public class AgentController {

    /*@PostMapping(path = "/save")
    public String saveAgent(@RequestBody AgentDTO agentDTO){
        String id = agentService.addAgent(agentDTO);
        return id;
    }*/
}
